var mi_numero = 1
var mi_cadena = "ejemplo"
document.write(mi_cadena)
/* Comentario */
function calcula (numero) {
	
	switch (numero)
{
	case 1:
	case 0: document.write ("El factorial de ", numero, " siempre es 1.\n") break
	default:
		if (numero < 0)
		{
			promt
		}
}

	return numero + mi_numero
}